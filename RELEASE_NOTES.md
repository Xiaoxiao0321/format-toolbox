# 格式转换工具箱 1.0.8

本次更新主窗口布局，并保留已发布的滚轮优化。

## 主窗口

- 宽窗口以文件区和设置栏双栏排列，任务队列与历史记录位于下方；窄窗口改为入口、设置、文件、结果的纵向排列。
- 常用功能入口集中在顶部，其余入口收在“更多功能”菜单。目标格式、输出目录和常用参数保持可见，高级参数可折叠；切换功能或目标格式时折叠高级参数并保留填写值。
- 文件区和设置区、工作区和结果区可通过分隔线调整尺寸；长输入路径省略显示并保留完整路径提示。
- 列表按像素滚动并回收行容器，滚动到列表边缘后由外层页面继续滚动。

## 验证与限制

- 发布构建的 74 项测试全部通过；180 次 PDF 压力转换全部成功、没有临时文件残留；安装、启动、离线依赖和卸载冒烟均通过。验收脚本未重复运行构建阶段测试。
- 安装包约 75.8 MiB，未配置数字签名；SHA-256 为 `CD6B514B81AB062E58F851152FB51C1E4EADE893D1D85D425DCAF4699AE7D3E5`。
- 窄屏布局验收采用模拟工作区；真实系统缩放和文件选择器仍需桌面验证。
- 1.0.8 安装包和验收报告生成于 `artifacts/installer`、`artifacts/acceptance`。

# 格式转换工具箱 1.0.7

本次更新清透莫兰迪青蓝界面，沿用 1.0.6 的转换功能。

## 界面与图标

- 本次重打包保留滚轮优化：列表按像素滚动并回收行容器，到达边缘后继续滚动外层窗口。

- 建立共享浅色主题，统一主窗口、PDF 页面工具、确认、反馈与关于窗口。
- 统一字体、圆角、边框和按钮、输入框、下拉框、复选框、菜单、列表、标签页、滚动条与提示框的视觉状态。
- 主操作使用深青蓝按钮，取消、关闭使用浅色描边按钮；选中列表使用浅青蓝背景和深色正文，禁用控件保持文字可读。
- 为功能入口与常用操作补充矢量线条图标；保留文件和转换箭头的软件标识造型，更新 PNG 和七种尺寸 ICO，同步安装与卸载图标。
- 增加输入文件区高度、合并按钮宽度和确认弹窗最小高度，减少裁切并给长提示保留滚动空间。

## 验证与限制

- 发布构建阶段 73 项测试全部通过；文字对比度检查及五个窗口的 100%、125%、150% 像素密度预览通过。发布验收未重复执行构建阶段已通过的测试。
- 180 次 PDF 压力转换全部成功，没有残留临时文件；安装、启动、离线依赖检查和卸载冒烟全部通过，测试安装目录已移除。
- 安装包约 75.8 MiB，当前未配置数字签名；安装包旁的 `.exe.sha256` 文件提供 SHA-256 校验值。
- 像素密度预览不替代 Windows 三种系统缩放的完整实机验收；完整交互验收仍有未覆盖项，见 `docs/ui-theme.md`。
- 发布验收详情见 `artifacts/acceptance/release-acceptance-1.0.7.json`。

# 格式转换工具箱 1.0.6

本次版本汇总当前转换可靠性、图片兼容性与界面适配更改。

## 任务处理与历史

- PDF 拆分接入主任务队列，支持进度、取消、重试、打开结果目录及历史记录。
- 历史保存完整转换参数，OCR 重新添加可恢复语言、页码、DPI 和灰度设置；支持按原参数重跑。旧历史兼容读取，缺失参数无法恢复。
- 多文件输出失败或取消时显示“部分成功”、警告和已生成结果，方便检查与继续处理。
- 退出时先取消任务，等待 Worker、临时文件清理与历史写入；Worker 优先关闭文档，超时后处理本次创建的自动化进程。

## PDF、图片与 OCR

- 添加文件的默认筛选包含 PDF，并提供独立 PDF 筛选。
- PDF 导图与 OCR 页面渲染在后台执行，主窗口持续刷新进度；JPEG 导出使用设置的质量。
- 多页 TIFF 按帧导出图片，转换为 TIFF、PDF、合并及 OCR 时保留全部页面。
- 静态 WebP 支持内置解码和透明度保留；动态或损坏 WebP 明确报错。
- 转换设置按输入和输出类型显示，无关参数不阻止任务执行。

## WPS 与界面

- 修复 WPS 长表格纵向分页设置；当 Excel 兼容组件由 WPS 提供时优先使用 WPS 原生组件，显示实际转换引擎，减少列截断及数字显示为 `###` 的问题。
- 关于、反馈、PDF 整理、确认弹窗根据屏幕工作区及 DPI 调整尺寸，支持按钮换行和长内容滚动。

## 验证与限制

- 发布构建：71 项测试全部通过；180 次 PDF 压力转换全部成功，无残留临时文件。
- 安装冒烟：安装、启动、依赖检查和卸载均通过，卸载后安装目录已移除。发布验收未重复执行已在构建阶段通过的测试。
- 安装包约 75.7 MiB，当前未配置数字签名；SHA-256 与验收详情见 `artifacts/acceptance/release-acceptance-1.0.6.json`。
- 新增任务流程、多页图片、转换回归及可复现生成样本验收。
- 生成样本覆盖多种 TIFF、WebP、长表格、大型扫描 PDF、OCR 字符错误率与模拟小屏幕弹窗；执行方式见 `tests/TestCorpus/README.md`。
- AutoCAD 布局仍需在安装完整版 AutoCAD 的环境验收；模拟工作区检查不代表系统弹窗已完成真实低分辨率与高 DPI 验收。
- 本版本安装包及发布验收报告位于 `artifacts/installer` 和 `artifacts/acceptance`。
